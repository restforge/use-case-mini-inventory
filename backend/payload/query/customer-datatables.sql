SELECT a.customer_id,
       a.customer_code,
       a.customer_name,
       a.contact_person,
       a.phone,
       a.email,
       a.address,
       a.city,
       a.country,
       a.is_active
FROM customer a