SELECT a.contact_id,
       a.contact_code,
       a.contact_name,
       a.contact_type,
       a.email,
       a.phone,
       a.address,
       a.city,
       a.country,
       a.is_active
FROM contact a
